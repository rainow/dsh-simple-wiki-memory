# 用户全局指令 / 持久记忆索引（DSWM）

> 本文件由 DSH 的 `dsh-agent-instructions` 机制在每个会话第一个请求前自动注入。
> 由 dsh-simple-wiki-memory (DSWM) 插件维护。规则区可被插件升级时更新；下方「记忆索引」区为用户数据，插件不覆盖。

## 持久记忆维护规则（六分支）

> 触发词中英文等价：下面所有中文指令（"存档/确认"、"整理记忆"）与英文（"save"/"confirm"/"promote"、"organize memory"/"cleanup"）含义相同，agent 应识别用户任意语言的等价表达。

### 1. 写入触发（会话末检查）
- 会话结束前，主动检查本次会话是否产生**值得长期记住**的信息（偏好、约定、决策、教训、环境事实等）；
- 有则按下面的准入流程处理，不要默默丢弃。

### 2. 准入流程（pending → 确认 → reference）
- **未确认的记忆**（包括你主导会话中临时产生的、以及一切无人值守会话产生的）→ 写入 `~/.dsh/workspace/pending/`，文件名标注来源与日期（如 `2026-08-18-plugins-update-note.md`）；
- `pending/` **不参与检索、不进本索引**，是待确认草稿区；
- **晋升**：你说"存档/确认"（或 "save"/"confirm"/"promote"）等指令时，agent 批量把 pending 条目移入 `~/.dsh/workspace/reference/`、更新本索引、并在 memory-log 记录；
- **TTL**：你主导会话产生的 pending 保留 7 天；无人值守会话产生的保留 30 天；到期未确认的在会话末提醒一次，不自动删除。

### 3. 无人值守会话（task-board 定时任务、后台 subagent）
- 只写 `pending/`（标注来源），**不自动晋升**、不直接写 reference；
- 你主导的会话**开始时**，agent 先汇报 pending 里有哪些待确认项。

### 4. 定期整理（用户发起 + 用户确认）
- 你说"整理记忆"（或 "organize memory"/"cleanup"）时，agent 扫描 `reference/` 全部文件与本索引，分析主题交叉/内容混杂/过时内容，输出重组方案（拆分/合并/改名/归档建议）；
- **你确认后**才执行；执行时更新索引与 memory-log；
- 过时/失效/被合并的内容 → 移到 `~/.dsh/workspace/archive/`（不参与检索、保留可查），不直接删除。

### 5. 备份（git 仓库）
- `~/.dsh/workspace/` 是 git 仓库；每次晋升、归档、整理后自动 commit；
- 不要删除 `.git/`。

### 6. 检索（索引 + 兜底）
- 新会话若任务与某主题相关，先 read 对应 reference 文件再动手；
- 索引保持指针式（主题 + 路径 + 一句话摘要），正文全部放 reference 文件；
- **兜底**：任务相关但索引无匹配条目时，扫一遍 `reference/` 目录确认，不要直接认定"没有记忆"。

### 通用约束
- 索引条目格式：`- [主题](~/.dsh/workspace/reference/主题.md) — 一句话摘要（日期）`（必须用完整路径）；
- 写入 `~/.dsh/workspace/` 需要 danger-full-access（或 workspace-write + 按需批准升级）；
- 每次写入/更新/晋升/归档在 `~/.dsh/workspace/memory-log.md` 追加一行（格式见该文件头）。

## 记忆索引

<!-- DSWM 插件在此下方合并用户的记忆索引条目；此注释行与上方规则区由插件管理 -->
